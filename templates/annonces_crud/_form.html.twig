{{ form_start(form) }}
<div class="form-edit-new-crud">
	{{ form_widget(form) }}
	<div class="flex-boutons-edit-new">
		<a class="back-list" href="{{ path('app_annonces_crud_index') }}">Annuler</a>
		<button class="confirm-btn">{{ button_label|default('Enregister') }}</button>
	</div>
</div>
{{ form_end(form) }}
