<div class="header">
	<header>
		<div class="logo-nav">
			<div class="name-img-logo">
				<a href="{{ path('app_home') }}">
					<img id="img-logo" src="/assets/images/maison.png" alt="IMAGE_LOGO">
					<p id="title">NOM_ENTREPRISE</p>
				</a>
			</div>
			<nav>
				<ul>
					<div class="liens-nav">
						<li>
							<a href="{{ path('app_home') }}">Accueil</a>
						</li>
						<li>
							<a href="{{ path('app_rent') }}">Louer</a>
						</li>
						<li>
							<a href="{{ path('app_purchase') }}">Acheter</a>
						</li>
						{% if app.user is not null and app.user.roles[0] == "ROLE_ADMIN" %}
							<li>
								<a href="{{ path('app_annonces_crud_index') }}">Gérér les annonces</a>
							</li>
							<li>
								<a href="{{ path('app_annonces_crud_new') }}">Ajouter un annonce</a>
							</li>
						{% endif %}
					</div>


					{% if app.user is null %}
						<div class="liens-log-nav">
							<li>
								<a href="{{ path('app_register') }}">Créer une compte</a>
							</li>
							<li>
								<a href="{{ path('app_login') }}">Se connecter</a>
							</li>
						</div>
					{% endif %}

					{% if app.user is not null %}
						<div class="liens-log-nav">
							<li>
								{# <img src="#" alt="photo-profil"> #}
								<a href="{{ path('app_favoris') }}">Mes favoris</a>
							</li>
							<li>
								{# <img src="#" alt="photo-profil"> #}
								<a href="{{ path('app_account') }}">Mon compte</a>
							</li>
						</div>

					{% endif %}
				</ul>
			</nav>
		</div>
	</header>
</div>
