<div class="header">
	<header>
		<div class="logo-nav">
			<div class="name-img-logo">
				<a href="home.html">
					<img id="img-logo" src="" alt="IMAGE_LOGO">
					<img id="title" src="" alt="NOM_SITE">
				</a>
			</div>
			<nav>
				<ul>
					<li>
						<a href="{{ path('app_home') }}">Accueil</a>
					</li>
                    <li>
						<a href="#">Annonces</a>
					</li>
                    
                    {% if app.user is null %}
					<li>
						<a href="{{ path('app_register') }}">Inscription</a>
					</li>
					<li>
						<a href="{{ path('app_login') }}">Connexion</a>
					</li>
                    {% endif %}

                    {% if app.user is not null %}
					<li>
						<a href="{{ path('app_account') }}">Profil</a>
					</li>
                    <li>
						<a href="{{ path('app_logout') }}">Deconnexion</a>
					</li>
                    {% endif %}
				</ul>
			</nav>
		</div>
	</header>
</div>
